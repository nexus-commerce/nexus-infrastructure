apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: api
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 80